export interface ServiceCardData {
    id: string
    title: string
    icon: string
    color: string
    description: string
    points: string[]
    tagline: string
    tagIcon: string
    featured?: boolean
}

export interface BrandData {
    name: string
    url: string
    logo: string
}
